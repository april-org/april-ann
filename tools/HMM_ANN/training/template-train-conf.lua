return {
  -- "@" for HTR, "( ) ." for ASR
  silences="@",
  tiedlist="tiedlist_CONCOMILLADOBLE.id",
  train_m="pats//list.train.mat",
  train_s="pats//list.train.sgm",
  train_f="pats//list.train.txt",
  val_m="pats//list.val.mat",
  val_s="pats//list.val.sgm",
  val_f="pats//list.val.txt",
  train_r=300000, val_r=200000,
  num_states=7,
  n=60, context=4, h1=192, h2=128,
  rndw=0.1, lr=0.001, mt=0.005, wd=1e-6,
  firstlr=0.005,
  epochs_firstlr=100,
  epochs_wo_val=4, epochs_wo_imp=20,
  epochs_max=100,
  epochs_first_max=500,
  feats_format="mat",
  feats_norm="pats/means_and_devs.PRESERVANDOX.lua",
  mean=0, var=0.015, seedp=86544,
  seed1=1234,
  seed2=4567,
  seed3=9876,
  epochs_wo_exp=5,
  em_it=100,
}
